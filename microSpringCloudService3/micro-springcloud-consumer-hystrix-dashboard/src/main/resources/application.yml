server:
  port: 9001
spring:
  application:
    name: dashoard1
eureka:
  instance:
    hostname: 7001.com #eureka服务端的实例名称
    prefer-ip-address: true
  client:
    register-with-eureka: false #false表示不向注册中心注册自己
    fetch-registry: false #false 表示自己就是注册中心，职责就是维护服务实例，并不需要去检索服务